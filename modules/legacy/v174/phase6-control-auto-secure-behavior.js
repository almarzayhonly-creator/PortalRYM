
/* FASE 6: Control de Auto completo + acceso ENA auditable + consulta ENA protegida */
function phase6Ago(v){if(!v)return '';const t=new Date(v).getTime();if(!Number.isFinite(t))return '';let s=Math.max(0,Math.round((Date.now()-t)/1000));if(s<60)return `hace ${s}s`;let m=Math.round(s/60);if(m<60)return `hace ${m} min`;let h=Math.round(m/60);return `hace ${h} h`}
function phase6EnaUser(n){const x=String(n||'').replace(/\D/g,'');return x?x.padStart(10,'0'):''}
function phase6StatusText(s){const n=norm(s);if(n==='CERRADO')return `<span class="ca6-status-alert">${esc(s)}</span>`;return v12Status(s)}
async function phase6RevealCredential(pan,host){
  const btn=host.querySelector('[data-ca6-reveal]'),out=host.querySelector('[data-ca6-secret]');if(!btn||!out)return;
  btn.disabled=true;btn.textContent='Consultando...';
  try{const {data}=await req('/functions/v1/ena-credencial-visible',{method:'POST',body:JSON.stringify({panapass:Number(pan)})});if(!data?.ok)throw Error(data?.error||'No se pudo obtener la credencial.');out.innerHTML=`<div><b>Usuario ENA:</b> <span class="ca6-secret">${esc(phase6EnaUser(data.panapass_numero))}</span></div><div><b>Clave ENA:</b> <span class="ca6-secret">${esc(data.password)}</span></div><div class="muted" style="margin-top:7px">Acceso mostrado bajo tu sesión y registrado para auditoría.</div>`;btn.textContent='Ocultar clave';btn.disabled=false;btn.onclick=()=>{out.innerHTML='';btn.textContent='Ver clave ENA';btn.onclick=()=>phase6RevealCredential(pan,host)}}catch(e){out.innerHTML=`<div class="alert">${esc(e.message||e)}</div>`;btn.disabled=false;btn.textContent='Ver clave ENA'}
}
function phase6OpenUnit(r){
 let m=document.querySelector('#ca6UnitModal');if(!m){m=document.createElement('div');m.id='ca6UnitModal';m.className='user-modal';document.body.appendChild(m)}
 const tags=Array.isArray(r.tags_detalle)?r.tags_detalle:[];
 const item=(label,val,wide=false)=>`<div class="ca6-detail-item ${wide?'ca6-detail-wide':''}"><span>${esc(label)}</span><b>${esc(val??'')}</b></div>`;
 m.style.display='flex';m.innerHTML=`<div class="user-modal-card" style="max-width:1180px;width:95vw;max-height:92vh;overflow:auto"><div class="table-summary"><div><h2 style="margin:0">${esc(r.unidad||'Unidad')}</h2><span class="muted">Control de Auto · ${esc(r.estatus||'')}</span></div><button class="soft-btn" id="ca6Close">Cerrar</button></div><div class="ca6-detail-grid">${item('Estatus',r.estatus)}${item('Empresa operadora',r.empresa_operadora)}${item('Empresa dueña',r.empresa_duena)}${item('Unidad',r.unidad)}${item('Placa única',r.placa_unica)}${item('Placa comercial',r.placa_comercial)}${item('Mes revisado',r.mes_revisado)}${item('Chasis',r.chasis,true)}${item('Motor',r.motor)}${item('Transmisión',r.transmision)}${item('Color',r.color)}${item('Marca',r.marca)}${item('Modelo',r.modelo)}${item('Año',r.anio)}${item('PIN',r.pin)}${item('Panapass',r.panapass_numero)}${item('TAG principal',r.tag)}${item('Estatus Netsuite',r.estatus_netsuite)}${item('Supervisora',r.supervisora)}${item('Galera',r.galera)}${item('Estado ENA',r.ena_estado_acceso)}${item('Saldo ENA guardado',r.ena_saldo==null?'':money(r.ena_saldo))}${item('Última consulta ENA',r.ena_ultima_consulta?fmtDT(r.ena_ultima_consulta):'')}${item('Cantidad TAG ENA',r.cantidad_tags)}${item('TAGs ENA',r.tags_ena||'',true)}</div><div class="ca6-ena-card"><div class="table-summary"><div><b>Acceso al portal ENA</b><div class="muted">Úsalo cuando necesites revisar Historial de Pases directamente en ENA.</div></div>${r.credencial_disponible?`<button class="soft-btn" data-ca6-reveal="${esc(r.panapass_numero||'')}">Ver clave ENA</button>`:`<span class="pill">Sin credencial disponible</span>`}</div><div data-ca6-secret></div></div>${tags.length?`<div class="card"><h3>TAGs asociados en ENA</h3><div class="table-wrap"><table class="pretty"><thead><tr><th>TAG</th><th>Estado</th><th>Financiero</th><th>Tipo</th><th>Vehículo</th><th>Matrícula</th><th>Corregimiento</th></tr></thead><tbody>${tags.map(t=>`<tr><td>${esc(t.tag||'')}</td><td>${esc(t.estado||'')}</td><td>${esc(t.estado_financiero||'')}</td><td>${esc(t.tipo_tag||'')}</td><td>${esc(t.tipo_vehiculo||'')}</td><td>${esc(t.matricula||'')}</td><td>${esc(t.corregimiento||'')}</td></tr>`).join('')}</tbody></table></div></div>`:''}</div>`;
 m.querySelector('#ca6Close').onclick=()=>m.style.display='none';m.onclick=e=>{if(e.target===m)m.style.display='none'};m.onkeydown=e=>{if(e.key==='Escape')m.style.display='none'};m.tabIndex=-1;m.focus();const b=m.querySelector('[data-ca6-reveal]');if(b)b.onclick=()=>phase6RevealCredential(r.panapass_numero,m);
}

v11UnitList=async function(){
 state.active='dashboard';shell();const v=document.querySelector('#view');
 v.innerHTML=`<div class="source-card"><span class="entity-chip">CONTROL DE AUTO</span><div class="source-text"><strong>Flota bajo tu alcance</strong><p>Las unidades activas son la vista principal. Cerradas y canibalizadas permanecen disponibles como control histórico.</p></div></div><div id="ca6Summary"></div><div class="ca6-tabs"><button id="ca6Active" class="active">Unidades activas</button><button id="ca6Other" class="soft-btn">Cerradas / canibalizadas</button></div><div class="section-tools"><div class="field"><label>Buscar unidad</label><input id="ca6Q" placeholder="Unidad; también reconoce placa, empresa, Panapass o TAG" autocomplete="off"></div><button id="ca6Go">Buscar</button></div><div id="ca6Out"><div class="card">Cargando Control de Auto...</div></div>`;
 let group='ACTIVAS',seq=0,timer=null,last=[];const out=document.querySelector('#ca6Out'),q=document.querySelector('#ca6Q');
 const sum=await rpc('panapass_control_auto_resumen').catch(()=>[]);const z=sum?.[0]||{},totalFlota=Number(z.activas||0)+Number(z.otros||0);document.querySelector('#ca6Summary').innerHTML=`<div class="ca6-summary"><div class="kpi"><span>Total flota</span><strong>${totalFlota}</strong></div><div class="kpi"><span>Activas</span><strong>${Number(z.activas||0)}</strong></div><div class="kpi"><span>Cerradas</span><strong style="color:var(--red)">${Number(z.cerradas||0)}</strong></div><div class="kpi"><span>Canibalizadas</span><strong>${Number(z.canibalizadas||0)}</strong></div></div><p class="v125-count-equation"><b>${totalFlota}</b> = ${Number(z.activas||0)} activas + ${Number(z.cerradas||0)} cerradas + ${Number(z.canibalizadas||0)} canibalizadas</p>`;
 const draw=rows=>{last=rows||[];out.innerHTML=`<div class="panel"><div class="table-wrap"><table class="pretty ca6-main-table"><thead><tr><th>Estatus</th><th>Unidad</th><th>Placa</th><th>Panapass</th><th>TAG</th><th>Empresa</th><th>Supervisora</th><th>Galera</th><th>Marca / Modelo</th><th>ENA</th><th></th></tr></thead><tbody>${last.length?last.map((r,i)=>`<tr><td data-label="Estatus">${phase6StatusText(r.estatus)}</td><td data-label="Unidad">${v17UnitBadge(r.unidad,r.color)}</td><td data-label="Placa">${esc(r.placa_unica||r.placa_comercial||'')}</td><td data-label="Panapass">${esc(r.panapass_numero||'')}</td><td data-label="TAG">${esc(r.tag||'')}</td><td data-label="Empresa">${esc(r.empresa_operadora||r.empresa_duena||'')}</td><td data-label="Supervisora">${esc(r.supervisora||'')}</td><td data-label="Galera">${esc(r.galera||'')}</td><td data-label="Marca / Modelo">${esc([r.marca,r.modelo,r.anio].filter(Boolean).join(' '))}</td><td data-label="ENA">${r.credencial_disponible?'<span class="chip ok">Credencial OK</span>':'<span class="muted">Sin credencial</span>'}</td><td data-label="Detalle"><button class="soft-btn" data-ca6-row="${i}">Ver ficha</button></td></tr>`).join(''):`<tr><td colspan="11" class="empty">Sin coincidencias.</td></tr>`}</tbody></table></div></div>`;out.querySelectorAll('[data-ca6-row]').forEach(b=>b.onclick=()=>phase6OpenUnit(last[Number(b.dataset.ca6Row)]))};
 const run=async()=>{const my=++seq;out.innerHTML='<div class="card">Consultando Control de Auto...</div>';try{const rows=await rpc('panapass_control_auto_v2',{p_grupo:group,p_buscar:String(q.value||'').trim()||null,p_limit:String(q.value||'').trim()?500:5000});if(my===seq)draw(rows)}catch(e){if(my===seq)out.innerHTML=`<div class="alert">${esc(e.message||e)}</div>`}};
 const setGroup=g=>{group=g;document.querySelector('#ca6Active').className=g==='ACTIVAS'?'active':'soft-btn';document.querySelector('#ca6Other').className=g==='OTROS'?'active':'soft-btn';run()};
 document.querySelector('#ca6Active').onclick=()=>setGroup('ACTIVAS');document.querySelector('#ca6Other').onclick=()=>setGroup('OTROS');document.querySelector('#ca6Go').onclick=run;q.onkeydown=e=>{if(e.key==='Enter'){e.preventDefault();run()}};q.oninput=()=>{clearTimeout(timer);timer=setTimeout(run,280)};await run();
};

async function phase6ConsultarSaldoENA(panapass,btn){
 if(!panapass||btn.disabled)return;const old=btn.textContent;btn.disabled=true;btn.textContent='Consultando...';let box=btn.parentElement.querySelector('.ena-saldo-box');if(!box){box=document.createElement('div');box.className='ena-saldo-box';btn.parentElement.appendChild(box)}box.innerHTML='Verificando disponibilidad y saldo ENA...';
 try{const {data}=await req('/functions/v1/ena-consulta-saldo',{method:'POST',body:JSON.stringify({panapass:Number(panapass)})});const r=data?.results?.[0];if(!data?.ok||!r)throw Error(data?.error||'No se pudo consultar ENA.');if(r.error==='credential_not_configured'){btn.disabled=true;btn.textContent='Sin credencial';btn.classList.add('ca6-disabled');box.innerHTML='<span class="muted">Todavía no tenemos credencial ENA para esta cuenta.</span>';return}if(!r.ok)throw Error(r.error||r.result||'ENA no devolvió saldo.');if(r.result==='BUSY'){const saldo=String(r?.summary?.saldo_texto??'').trim();box.innerHTML=`<span class="ena-busy">Consulta ya en proceso.</span>${saldo?`<br>Último saldo guardado: <b>${esc(saldo)}</b>`:''}${r.ultima_consulta?`<span class="ena-age">${esc(phase6Ago(r.ultima_consulta))}</span>`:''}`;return}if(r.result!=='OK')throw Error(r.result||'ENA no devolvió saldo.');const saldo=String(r?.summary?.saldo_texto??'').trim()||'N/D';box.innerHTML=`<b>Saldo ENA: ${esc(saldo)}</b><br><span class="${r.cached?'ena-cache':'muted'}">${r.cached?'Última consulta ENA':'Consulta realizada ahora'}${r.ultima_consulta?` · ${esc(phase6Ago(r.ultima_consulta))}`:''}</span>`}catch(e){box.innerHTML=`<span style="color:var(--red)">${esc(e.message||e)}</span>`}finally{if(!btn.classList.contains('ca6-disabled')){btn.disabled=false;btn.textContent=old}}
}

const _phase6NegBase=negativos;
negativos=async function(v){
 await _phase6NegBase(v);
 const enhance=async()=>{const buttons=[...v.querySelectorAll('[data-ena-saldo]')];const nums=[...new Set(buttons.map(b=>Number(b.dataset.enaSaldo)).filter(Boolean))];if(!nums.length)return;let rows=[];try{rows=await rpc('panapass_ena_disponibilidad',{p_panapass:nums})}catch(e){console.warn('Disponibilidad ENA',e);return}const map=new Map((rows||[]).map(x=>[String(x.panapass_numero),x]));buttons.forEach(b=>{const d=map.get(String(b.dataset.enaSaldo));const cell=b.parentElement;if(!d?.credencial_disponible){b.disabled=true;b.textContent='Sin credencial';b.classList.add('ca6-disabled');b.title='No se consultará ENA porque aún no existe credencial activa.';if(d?.ena_saldo!=null){let box=cell.querySelector('.ena-saldo-box');if(!box){box=document.createElement('div');box.className='ena-saldo-box';cell.appendChild(box)}box.innerHTML=`Último saldo guardado: <b>${money(d.ena_saldo)}</b>${d.ena_ultima_consulta?`<span class="ena-age">${esc(phase6Ago(d.ena_ultima_consulta))}</span>`:''}`}}else{b.classList.remove('ca6-disabled');b.onclick=()=>phase6ConsultarSaldoENA(b.dataset.enaSaldo,b);b.dataset.enaLast=d?.ena_ultima_consulta||'';const last=d?.ena_ultima_consulta?new Date(d.ena_ultima_consulta).getTime():0;const remain=last?Math.max(0,1800000-(Date.now()-last)):0;if(remain>0){b.disabled=true;b.textContent=`Disponible en ${Math.ceil(remain/60000)} min`;b.title='Esta unidad ya fue consultada en ENA dentro de los últimos 30 minutos.'}else{b.disabled=false;b.textContent='Consultar saldo';b.title='Consulta rápida: solo saldo ENA.'}if(d?.ena_saldo!=null&&d?.ena_ultima_consulta){let age=cell.querySelector('.ena-age');if(!age){age=document.createElement('span');age.className='ena-age';cell.appendChild(age)}age.textContent='Última: '+phase6Ago(d.ena_ultima_consulta)}}})};
 await enhance();
 const go=v.querySelector('#p3NegGo');if(go){const old=go.onclick;go.onclick=async()=>{await old?.();setTimeout(enhance,80)}}const date=v.querySelector('#p3NegFecha');if(date){const old=date.onchange;date.onchange=async()=>{await old?.();setTimeout(enhance,80)}};
};

const _phase6Dashboard=dashboard;
dashboard=async function(v){await _phase6Dashboard(v);try{const s=(await rpc('panapass_control_auto_resumen'))?.[0]||{},total=Number(s.activas||0)+Number(s.otros||0);const card=document.createElement('div');card.className='card ca6-dashboard-card';card.innerHTML=`<div><b>Control de Auto</b><div class="muted">Vista principal: unidades activas. Cerradas y canibalizadas permanecen como control histórico.</div></div><div class="ca6-dashboard-stats"><span>${total} unidades</span><span>${Number(s.activas||0)} activas</span><span>${Number(s.cerradas||0)} cerradas</span><span>${Number(s.canibalizadas||0)} canibalizadas</span><button id="ca6DashOpen">Abrir Control de Auto</button></div>`;v.prepend(card);card.querySelector('#ca6DashOpen').onclick=v11UnitList}catch(e){console.warn('Control Auto dashboard',e)}};
