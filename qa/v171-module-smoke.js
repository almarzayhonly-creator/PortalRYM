const { chromium } = require('playwright');
(async()=>{const url=process.env.QA_URL;if(!url)throw new Error('QA_URL missing');const browser=await chromium.launch({headless:true});const page=await browser.newPage();const errors=[];page.on('pageerror',e=>errors.push(String(e)));await page.goto(url,{waitUntil:'domcontentloaded',timeout:45000});const state=await page.evaluate(async()=>{if(!window.RYM_V171_READY)return {ready:false};try{return {ready:true,data:await window.RYM_V171_READY}}catch(e){return {ready:false,error:String(e)}}});console.log(JSON.stringify({state,errors},null,2));await browser.close();if(!state.ready)process.exit(1);const expected=['panapass','revisados','control-auto','gps','usuarios'];if(expected.some(x=>!state.data.modules.includes(x)))process.exit(1)})().catch(e=>{console.error(e);process.exit(1)});
