# V172 final closure scope

1. Persist and renew authenticated session through the canonical core session layer.
2. Remove remaining Control Auto V75 renderer ownership from the monolith without changing behavior.
3. Run architecture QA, then browser preview QA before any production decision.

Production/main remain out of scope until explicit approval.
